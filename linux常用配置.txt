==================================修改系统并发数===============================
# ulimit -n	[number]//number为设置并发数，不带number查看并发数，默认为1024。
===============================================================================

=======================设置自启动=========================
把启动程序的命令添加到/etc/rc.d/rc.local文件中
==========================================================